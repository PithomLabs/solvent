# Post-Fable Remediation — Final Receipt

**Date:** 2026-08-10
**Status:** Complete. Awaiting Technical Lead review.

---

## Summary

All documentation/receipt items from the Fable rework verification are resolved.
No kernel, schema, pipeline, normalize, derive, belief, intent, or Wave 1–4
architecture changes were made. The code is verified; the evidence trail now
matches.

---

## Finding Resolution

### F-3 — Documentation completion

EnsureBelief added to README kernel public surface list (`README.md:59-62`).
`IMPLEMENTATION_CONTRACT.md` §4 was already correct. "Seven functions" → "eight
functions" corrected in `docs/M1_REPORT.md:21,33`, `docs/M2_REPORT.md:74`, and
`docs/M1_review.md:13,98`.

### F-5 / F-8 — Receipt completion

M2 receipts regenerated from a single `m2_accept.sh` run. The committed pair is
now coherent:

- `docs/M2_TRANSCRIPT.md` = **28/28 cases passed** (28 rows)
- `docs/M2_TEST.md` = **27 top-level PASS per run**, twice (54 total `--- PASS`)
- B-21 observation: transcript says `returned 2 in 5 ms`; run 2 says
  `returned 2 in 5 ms` — **match confirmed**

`docs/M2_REPORT.md` updated: 25/25 → 28/28, "25 rows" → "28 rows", wave 3
count 3 → 6 (adding W3-Ensure-New, W3-Ensure-Existing, W3-Ensure-DiffScenario).

`docs/M1_REPORT.md` updated: 8/8 → 9/9 (PREPARE), "6 ExecuteTx" → "7 ExecuteTx"
(I-7), "8 catalog statements" → "9 catalog statements".

`docs/M1_I7.md` and `docs/M1_PREPARE.md` regenerated by the acceptance machinery.

### F-23 — README completion

- "seven functions" → EnsureBelief added to kernel surface list
- "84 tests" → "85 tests" (3 locations)
- Wave 1 "11" → "12"
- CLI examples now include DSN requirement (`FABLE_DSN` or `--fixtures`)
- "atomically retract a belief and its cascade" → accurately describes
  single-belief retract + intent cancel; multi-generation cascade pointed to
  M2 B-07/B-20/B-22
- I-6 rewritten: "no embedding column" instead of "embedding columns NULL"
- Agent description: removed "merge duplicates by vector similarity" (Phase 2,
  not current)
- "Before you commit": "all embeddings NULL" → "zero vectors"

### N-1 — Test count resolved

`84` → `85` and Wave 1 `11` → `12` in:
- `README.md:135,140,147`
- `AGENTS.md:85,90`
- `PUBLIC_REPO_AUDIT.md:90,96,101,104,105`
- `plans/post/fix.md:197`

### N-2 — Kernel surface resolved

`EnsureBelief` added to README kernel public surface list.

### N-3 — CLI examples resolved

Both CLI examples now show `FABLE_DSN` / `--dsn` and `--fixtures` usage.

### N-5 — Implemented

`cmd/solvent/main.go:205-211`: `runFalsify` now prints `VERDICT: NO-OP` when
`retracted == 0`, distinguishing no-op falsifications from successful ones.
The audit check (`auditCount != 0`) remains unchanged.

### N-6 — Resolved

README cascade claim softened. CLI documentation now accurately describes what
the shipped binary demonstrates (single-belief retract + intent cancel).
Multi-generation cascade capability documented as evidenced by M2 B-07/B-20/B-22.

### Embedding leftovers resolved

Three leftover embedding/vector references in README removed or rewritten:
- Line 34: I-6 → "zero vectors" (no embedding column)
- Line 72: agent description → removed "vector similarity before insert"
- Line 148: "all embeddings NULL" → "zero vectors"

---

## Final State

### Test count

**85 tests, all passing.**

| Package | Tests |
|---|---|
| internal/kernel | 27 |
| internal/belief | 18 |
| internal/derive | 16 |
| internal/normalize | 12 |
| internal/intent | 3 |
| internal/pipeline | 9 |
| **Total** | **85** |

### M1 results

- `m1_accept.sh` exit 0
- PREPARE sweep: **9/9** statements prepared
- I-7: **7** ExecuteTx write sites, 0 raw writes
- Build: exit 0, vet: exit 0, gofmt: clean

### M2 results

- `m2_accept.sh` exit 0
- Behavioral suite: **28/28** cases passed, twice consecutively
- Wave 0: 1, Wave 1: 13, Wave 2: 8, Wave 3: 6

### M2 receipt relationship

The committed `M2_TRANSCRIPT.md` and `M2_TEST.md` are from the **same** final
`m2_accept.sh` run. Evidence:

- Transcript B-21: `returned 2 in 5 ms`
- M2_TEST run 2 B-21: `returned 2 in 5 ms`
- Both 28/28, 27 top-level PASS per run

After `m2_accept.sh` completed, no `go test ./internal/kernel` or equivalent
was run that would overwrite the transcript.

### CLI verification

- `cmd/solvent --reset --scenario …` → `VERDICT: PASS`, audit 0
- `cmd/solvent -falsify <id> --scenario …` → 1 belief retracted, intent
  cancelled, audit 0, `VERDICT: PASS`
- `cmd/solvent -falsify <wrong-scenario-id> --scenario …` → retracted 0,
  `VERDICT: NO-OP`
- Replay determinism: byte-identical

---

## Deferred Findings (unchanged)

These Fable findings remain deferred per agreement. None regressed:

- F-10: `go test ./internal/kernel` overwrites `docs/M2_TRANSCRIPT.md`
- F-11: Latent same-CVE aggregation gap
- F-12: Theoretically unreachable SHA marshal failure
- F-14: Concurrent EnsureBelief limitation (SERIALIZABLE assumed)
- F-15: M0 probe A3 receipt wording discrepancy
- F-16: GitHub issue body parsing (title-only)
- F-17: Stronger sentinel assertions in tests
- F-18: Reset-lock timeout
- F-19: Mutable FullDebt
- F-20: Dead code
- N-4: cliReset drops DB before schema resolution
- N-7: Duplicated parsePGArray test helper
- N-8: Assets-less release fallback URL
- N-9: M0_TRANSCRIPT.md lost elapsed_ms column

---

## Verification Checklist

- [x] `go build ./...` — exit 0
- [x] `go vet ./...` — exit 0
- [x] `gofmt -l cmd internal` — empty
- [x] `scripts/check_i7.sh` — I-7 PASS: 7 sites
- [x] `scripts/m1_accept.sh` — exit 0
- [x] `scripts/m2_accept.sh` — exit 0, 28/28 twice
- [x] Non-kernel tests pass (belief, derive, intent, normalize, pipeline)
- [x] M2_TRANSCRIPT.md = 28/28
- [x] M2_TEST.md = 27 PASS per run × 2
- [x] B-21 observation matches between transcript and run 2
- [x] README CLI examples include DSN
- [x] README says four tables, no embedding column
- [x] README kernel surface includes EnsureBelief
- [x] M1/M2 reports: 9/9, eight functions, 28/28
- [x] Stale count search clean for target files
- [x] Git status and diff reviewed

---

Do NOT commit to GitHub.
Do NOT push.
Stop. Wait for Technical Lead review.
