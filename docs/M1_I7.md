# M1 I-7 Report

Invariant I-7: *"Every kernel write goes through `crdb.ExecuteTx`. No raw
`db.Exec`/`db.Query` writes."* Contract §7 M1 states this as a grep criterion;
this report is that grep.

## Verdict

**PASS** — 7 `crdb.ExecuteTx` write sites, 0 raw writes, 1 permitted pool read.

## Write sites

Each statement below executes on a `*sql.Tx` supplied by `crdb.ExecuteTx`.
The type is the proof: a `*sql.Tx` cannot be obtained outside the closure.

| function | line | method | statement |
|---|---|---|---|
| `EnterBelief` | 46 | `tx.QueryRowContext` | `sqlEnterBelief` |
| `AddEvidence` | 61 | `tx.ExecContext` | `sqlAddEvidence` |
| `RetireDebt` | 71 | `tx.ExecContext` | `sqlRetireDebt` |
| `Promote` | 83 | `tx.ExecContext` | `sqlPromote` |
| `IntentOnPromoted` | 95 | `tx.ExecContext` | `sqlIntentOnPromoted` |
| `RetractCascade` | 117 | `tx.ExecContext` | `sqlRetractCascadeCancel` |
| `RetractCascade` | 121 | `tx.ExecContext` | `sqlRetractCascadeRetract` |
| `EnsureBelief` | 158 | `tx.QueryRowContext` | `sqlEnsureBelief` |

## crdb.ExecuteTx call sites

| function | line |
|---|---|
| `EnterBelief` | 45 |
| `AddEvidence` | 60 |
| `RetireDebt` | 70 |
| `Promote` | 82 |
| `IntentOnPromoted` | 94 |
| `RetractCascade` | 115 |
| `EnsureBelief` | 157 |

## Permitted exception

`AuditLiveOnNonPromoted` reads through the pool without a transaction. I-7 governs
writes; this function performs none. It is the only `s.db.*` call in the package.

## D-033

`CREATE TEMP TABLE` occurrences in `kernel`: **0**. The draft's temp-table cascade is
retired; `RetractCascade` uses the `WITH RECURSIVE` form proven by M0 probe D4.
