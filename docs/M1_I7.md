# M1 I-7 Report

Invariant I-7: *"Every kernel write goes through `crdb.ExecuteTx`. No raw
`db.Exec`/`db.Query` writes."* Contract §7 M1 states this as a grep criterion;
this report is that grep.

## Verdict

**PASS** — 6 `crdb.ExecuteTx` write sites, 0 raw writes, 1 permitted pool read.

## Write sites

Each statement below executes on a `*sql.Tx` supplied by `crdb.ExecuteTx`.
The type is the proof: a `*sql.Tx` cannot be obtained outside the closure.

| function | line | method | statement |
|---|---|---|---|
| `EnterBelief` | 57 | `tx.QueryRowContext` | `sqlEnterBelief` |
| `AddEvidence` | 72 | `tx.ExecContext` | `sqlAddEvidence` |
| `RetireDebt` | 82 | `tx.ExecContext` | `sqlRetireDebt` |
| `Promote` | 94 | `tx.ExecContext` | `sqlPromote` |
| `IntentOnPromoted` | 106 | `tx.ExecContext` | `sqlIntentOnPromoted` |
| `RetractCascade` | 128 | `tx.ExecContext` | `sqlRetractCascadeCancel` |
| `RetractCascade` | 132 | `tx.ExecContext` | `sqlRetractCascadeRetract` |

## crdb.ExecuteTx call sites

| function | line |
|---|---|
| `EnterBelief` | 56 |
| `AddEvidence` | 71 |
| `RetireDebt` | 81 |
| `Promote` | 93 |
| `IntentOnPromoted` | 105 |
| `RetractCascade` | 126 |

## Permitted exception

`AuditLiveOnNonPromoted` reads through the pool without a transaction. I-7 governs
writes; this function performs none. It is the only `s.db.*` call in the package.

## D-033

`CREATE TEMP TABLE` occurrences in `internal/kernel`: **0**. The draft's temp-table cascade is
retired; `RetractCascade` uses the `WITH RECURSIVE` form proven by M0 probe D4.
