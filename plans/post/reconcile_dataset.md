# TASK — RECONCILE THE REAL-WORLD ETCD DATASET INTO AN IMPLEMENTATION-READY DESIGN
#
# IMPORTANT: DESIGN ONLY. DO NOT IMPLEMENT OR MODIFY CODE YET.

We are preparing the next implementation phase of Solvent's real-world
etcd evidence integration.

You MUST first read:

1. The current repository:

2. The Gemini Deep Research report:
   evidence_dataset.md

3. The adversarial review:
   DATASET_ADVERSARIAL_REVIEW.md

The adversarial review is the authoritative correction of the Gemini
implementation specification.

Your job is NOT to defend Gemini's original design.

Your job is to reconcile the research with the ACTUAL CURRENT SOLVENT
CODEBASE and produce one final implementation-ready design document.

============================================================
CURRENT DECISION
============================================================

The research/data selection is approved in principle.

Keep the two-track real-world etcd story:

TRACK 1 — CVE-2026-33413 lifecycle

    GHSA/CVE-2026-33413
          ↓
    etcd v3.5.27 affected
          ↓
    etcd v3.5.28 fixed
          ↓
    promotion / intent / replay

TRACK 2 — historical deployment-decision retraction

    etcd v3.5.0
          ↓
    postulated deployment-approval decision
          ↓
    v3.5 data-inconsistency postmortem
          ↓
    RetractCascade
          ↓
    intent cancelled

Track 2 is a separate scenario_id and is framed as an
as-of-2022 historical replay.

The postmortem INVALIDATES A DEPLOYMENT DECISION.

It does NOT falsify the historical fact that v3.5.0 was released as GA.

============================================================
NON-NEGOTIABLE CORRECTIONS FROM FABLE
============================================================

Treat these as requirements:

1. READ db/001_schema.sql BEFORE DESIGNING ANY MAPPING.

The actual schema is authoritative.

Belief statuses:

    entered
    promoted
    retracted

Intent states:

    live
    cancelled
    executed

There is NO:

    DERIVED
    PENDING
    LEGAL

Do not invent them.

2. Debt is the existing six-item FullDebt vocabulary:

    needMap
    needInvariant
    needToyCheck
    needNullModel
    needObstruction
    needFaithfulnessReview

Do NOT invent domain-specific debts such as:

    "Fix Availability Debt"
    "Vendor Confirmation Debt"

Determine exactly how the proposed real evidence interacts with the
existing six-item debt model.

3. Do NOT use belief_edge for evidence invalidation.

Actual belief_edge semantics are:

    belief → belief
    kind ∈ {derives, contradicts}

There is no:

    SUPPORTS
    INVALIDATES

Evidence attaches to beliefs through evidence.belief_id.

Track 2 invalidation is:

    postmortem evidence attached to target belief
        +
    RetractCascade(targetBeliefID)

No INVALIDATES edge.

4. Intent gating is physical, not a pending state machine.

An intent cannot exist while its belief is unpromoted.

The actual behavior is:

    IntentOnPromoted(unpromoted belief)
        → ErrActionOnUnpromoted / FK failure

After debt is retired and the belief is promoted:

    IntentOnPromoted(promoted belief)
        → state = live

Do NOT invent a pending-to-legal transition.

5. Evidence schema must match db/001_schema.sql exactly.

Do not invent:

    evidence_type
    raw_payload
    source_id
    publication_date
    is_synthetic

Use the actual fields.

Database IDs are UUIDs generated by the database.

Any ev_*/bel_*/act_* identifiers are fixture-local labels only.

Evidence requires an existing belief, so the actual order is:

    normalize
      ↓
    derive
      ↓
    EnsureBelief
      ↓
    AddEvidence

6. content_sha256 is NOT a raw-payload hash.

The existing normalizers hash their normalized source-type subset.

The manifest may contain hashes of fixture files for supply-chain
integrity, but do not confuse those with evidence.content_sha256.

7. NO fabricated evidence.

Remove the proposed synthetic.local record.

Do not invent URLs, advisory IDs, or fake external evidence.

If operator_asserted evidence is genuinely needed to close existing debt,
design it explicitly as operator_asserted with a real internal reference
and no fabricated URL.

8. The original etcd_demo_fixture.json design is incompatible with the
existing pipeline.

Inspect the actual pipeline and normalizers.

Determine the minimum fixture/file structure that the existing pipeline
can actually consume.

Do not assume a single arbitrary envelope JSON file will work.

9. Track 2 needs the real etcd v3.5.0 release record.

The baseline deployment decision should be:

    claim_type = postulated

    "etcd v3.5.0 is approved for production deployment
     (decision as of 2021-06-16)"

It must be entered through the existing EnterBelief path.

Do not derive this claim from release metadata because the existing
release derivation does not produce stability/deployment-suitability
claims.

10. Track 2 must use its own scenario_id and be framed as an
as-of-2022 historical replay.

Do not create a misleading chronology where a 2022 postmortem appears
to arrive after 2026 release/CVE evidence.

11. Contradiction behavior must be described honestly.

Existing contradiction handling detects/logs contradiction and produces
no ledger mutation.

There is NO automatic promotion freeze.

Do not add one merely to make the demo more impressive.

12. All writes remain through crdb.ExecuteTx.

Do not introduce:

    BEGIN/COMMIT blocks at CLI level
    db.Exec writes
    alternative transaction handling

Run/check I-7.

============================================================
IMPORTANT SCOPE DECISION
============================================================

Fable identified that adding genuine etcd source types may require a small
additive extension to previously frozen Waves 1/2/4:

    normalize
    derive
    belief/mapping
    pipeline sourceTypeMap

This is potentially acceptable, but it must be EXPLICIT.

Determine exactly:

    - which files need changes
    - what new source types are required
    - what the smallest implementation is
    - whether the existing source types can honestly represent the records
    - whether any proposed extension is actually necessary

DO NOT silently unfreeze anything.

Do NOT modify schema.

Do NOT add tables.

Do NOT add infrastructure.

Do NOT add RAG/embeddings/vector search/live APIs.

If a Wave 1/2/4 extension is required, document it clearly as:

    ADDITIVE EXTENSION — TECHNICAL LEAD APPROVAL REQUIRED

Do not implement it in this task.

============================================================
KEY DESIGN QUESTION
============================================================

Determine the smallest implementation that gives us a credible demo.

We want the demo to show:

TRACK 1:

    real evidence
      ↓
    belief
      ↓
    debt/promotion behavior
      ↓
    action intent
      ↓
    replay/idempotence

TRACK 2:

    real historical evidence
      ↓
    postulated deployment decision
      ↓
    live intent
      ↓
    operator-triggered RetractCascade
      ↓
    cancelled intent + retracted belief
      ↓
    audit invariant

Do not force a four-scenario demo if the current kernel does not support
it.

The demo should demonstrate what Solvent ACTUALLY does.

============================================================
CRITICAL DEBT QUESTION
============================================================

Resolve this explicitly.

The existing six-item FullDebt means the original real-world dataset
cannot automatically reach promotion with only GHSA + release evidence.

Determine the minimum honest solution.

Consider the options identified by Fable:

A. Demonstrate promotion refusal with four debts remaining, then separately
   close the remaining debt using an existing legitimate mechanism.

B. Use real maintainer-confirmation artifacts if they genuinely exist and
   map to the existing debt retirement rules.

C. Use an explicitly labeled operator_asserted review record with a real
   internal reference if that is the smallest honest mechanism.

Do NOT invent evidence just to make promotion happen.

Recommend ONE approach and explain why.

============================================================
DELIVERABLE
============================================================

Create:

    plans/post/etcd_real_data_implementation_spec.md

Do NOT modify production code.

Do NOT create the fixture yet.

Do NOT commit anything.

The document must contain:

# 1. Executive Decision

State exactly what we are implementing.

# 2. Repository Reality

Document the actual relevant:

- schema
- statuses
- intent states
- debt vocabulary
- normalization
- derivation
- pipeline
- RetractCascade
- CLI

Cite exact files/functions/lines where practical.

# 3. Final Dataset

Exact final record list.

For each:

- real/operator_asserted
- source
- purpose
- scenario_id
- source type
- required fields
- provenance

Zero synthetic external records.

# 4. Track 1 Design

Exact end-to-end lifecycle.

Show:

    source
    normalization
    derivation
    belief
    debt
    promotion/refusal
    intent
    replay

Do not describe behavior that the current code does not implement.

# 5. Track 2 Design

Exact end-to-end lifecycle.

Show:

    v3.5.0 evidence
    postulated belief
    promotion
    live intent
    postmortem evidence
    --falsify
    RetractCascade
    cancelled intent
    audit

# 6. Exact Fixture Layout

Specify:

- directory structure
- filenames
- top-level JSON shape
- exact fields required by each normalizer
- manifest
- raw source snapshots
- attribution

The design must be directly compatible with the current pipeline.

# 7. Normalization Specification

For every new/modified source type:

- input shape
- normalized fields
- provenance class
- subject/version representation
- content hash behavior

# 8. Derivation Specification

For every source type:

- exact claim generated
- claim type
- subject
- scenario behavior
- debt retirement behavior

Do not invent claims that existing derivation cannot produce unless the
document explicitly marks the change as an approved additive extension.

# 9. Debt / Promotion Specification

Use ONLY the six existing FullDebt items.

Show exactly how each is retired.

Identify any required operator_asserted path.

# 10. Required Code Changes

List every file that must change.

Separate:

    EXISTING CODE REUSE
    ADDITIVE EXTENSION
    FORBIDDEN / NOT NEEDED

No implementation yet.

# 11. CLI / Demo Procedure

Use the ACTUAL current CLI.

Do not invent subcommands.

Specify exact commands conceptually, including:

    --scenario
    --reset
    --fixtures
    --out
    --falsify

where appropriate.

# 12. Acceptance Criteria

Must verify:

- non-zero records actually processed
- deterministic replay
- promotion/refusal behavior
- intent gating
- Track 2 retraction
- cancelled intent
- audit_live_on_nonpromoted = 0
- go build ./...
- go vet ./...
- gofmt
- scripts/check_i7.sh
- existing tests remain green

# 13. Scope / Deadline Check

Explicitly state:

- what is NOT being built
- why this remains MVP
- estimated implementation complexity
- any risk that could threaten August 18

# 14. Adversarial Review Prompt

At the end, include a ready-to-copy prompt for a separate adversarial
coding-review agent.

That prompt must instruct the reviewer to compare the eventual
implementation against THIS design document and the frozen repository
contracts.

============================================================
QUALITY BAR
============================================================

The design is successful only if a coding agent can read it and implement
the real-data integration without having to reinterpret:

- the schema
- debt semantics
- intent semantics
- provenance
- fixture format
- scenario behavior
- CLI behavior

If you discover that the proposed dataset cannot be integrated without
changing an existing invariant, STOP and report the conflict.

Do not solve it by silently changing the architecture.

Again:

    DESIGN ONLY.
    NO CODE.
    NO SCHEMA CHANGES.
    NO FIXTURE GENERATION.
    NO COMMITS.

The goal is to produce the final implementation contract that we can
approve once, then hand to the coding agent for execution.